#include <stdio.h>

int main() {

    // arithmetic opearators
    int a = 8;
    double b = a/3;
    printf("b = %lf\n", b); // b = 2.000000

    double x = 8;
    double y = x/3;
    printf("y = %lf\n", y); // y = 2.666667

    int z = 0;
    printf("z = %d\n", ++z);

    // logical operators
    // &&, ||, !

    // relational operators
    // ==, >=, >, ...

    return 0;
}
